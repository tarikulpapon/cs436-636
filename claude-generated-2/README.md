# Terminal 1 — backend
cd server && npm install && npm run dev

# Terminal 2 — frontend
cd client && npm install && npm run dev

Then open [http://localhost:5173](http://localhost:5173). The key things to swap in are your Supabase URL and anon key in server/index.js


